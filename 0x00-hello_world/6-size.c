#include <stdio.h>

/**
	* Main function 
	* Printing each type's size
	* Return (0)
	*/
int main(void)
{
	char a;
	int b;
	long int c;
	long long int d;
	float e;
	arr = [a,b,c,d,e];
	for(int i=0; i< arr.length;arr++)
	{
		printf(`Size of %d: %lu byte(s)\n`, $arr[i]  sizeOf($arr[i]));
	}
	return (0);
}

