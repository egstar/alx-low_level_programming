#include"main.h"
/**
	* _print_rev - Function description
	*
	*
	* @s: Parameter description
*/
void print_rev(char *s)
{
	int i;

	for (i = 0; s[index] != '\0'; ++index)
		;

	for (--i; i >= 0; --i)
		_putchar(s[i]);
	_putchar('\n');
}
